"use server"
import initiateRequest from '../utils/initiateRequest'

export default async function DashboardSelector() {
  // const res: any = await initiateRequest('/api/dashboard-selector')
  
  return (
    <main>
      <h1>Dashboard Selector</h1>
    </main>
  )
}
